# AeroOpt — разбор ТЗ по коду (walkthrough)

Дата: 2026-08-29 (итерация 2 — «всё сразу»)
Что проверено: весь код приложения (без license_client и без сборки .exe).

---

## 0. Коротко о том, что за приложение и как устроено

- **Стек:** Python 3.12 + PyQt5, pyvista/pyvistaqt (3D-превью), matplotlib, gmsh (сетка), numpy/scipy, SU2_CFD (решатель), mpiexec (MPI).
- **Точка входа:** `su2_gui.py` → `ui/main_window.py` (`MainWindow`). Дубль-вход `main.py` — просто прокси на тот же `main()`.
- **Модули:**
  - `config/` — настройки + полётные условия (ISA, пресеты самолётов)
  - `geometry/` — параметрические генераторы (фюзеляж, крыло, закрылки, предкрылки, ГО/ВО), «лечение» STL, **Direct CAD Import**
  - `mesh/` — объёмная тетра-сетка через gmsh + запись `mesh.su2` с маркерами (в т.ч. симметрии), **адаптация сетки**
  - `solver/` — сборка `config.cfg`, сессии, фоновые воркеры (в т.ч. **DOE/перебор вариантов**), гибридный CPU+GPU
  - `su2_autoconfig.py`, `su2_config_dialog.py` — устойчивые пресеты + окно настроек SU2 (восстановлены в репозиторий)
  - `optimization/` — правила проектирования (RuleSet), многоточечная оптимизация
  - `physics/` — атмосфера ISA, профили NACA, расчёт сил
  - `ui/` — главное окно (дерево проекта в стиле COMSOL)
- **Состояние кода:** здоровое. `tests/test_backend.py`, `tests/smoke_import.py`, `tests/ast_lint.py` — все зелёные (23 модуля импортируются).

---

## 1. Сводная таблица «пункт ТЗ → статус в коде»

| № | Требование ТЗ | Статус | Где реализовано |
|---|---|---|---|
| 1 | Многоядерные вычисления | ✅ Есть + улучшено | Выбор ядер/нагрузки CPU/GPU, mpiexec -n N, SU2_PARTITION, гибрид CPU+GPU с фоллбэком; добавлены **подсказка по числу ядер** при старте и **ETA до конца серии** |
| 2 | Плоскость симметрии как ГУ | ✅ Есть (полный цикл) | UI → маркеры symmetry_xy/xz/yz в mesh.su2 → MARKER_SYM, с защитой «нет маркера в сетке — не включаем» |
| 3 | Адаптивная сетка | ✅ Реализовано | `solver/workers.py:run_su2_adapt()` + `MeshAdaptWorker` + кнопка «🧬 Адаптировать сетку» (SU2_ADAPT по restart.dat, замена mesh.su2) |
| 4 | Direct CAD Import | ✅ Реализовано | `geometry/generators.py:cad_to_stl()` (gmsh OCC: STEP/IGES/BREP/… → STL), ветка в `_add_body`; CAD-файлы импортируются напрямую |
| 5 | Параметрическая оптимизация по таблице параметров | ✅ Реализовано | Таблица вариантов (DOE) в UI: строки = варианты, столбцы = span/chord_root/chord_tip/sweep; полный перебор через `OptimizationWorker(candidates=…)`; таблица результатов + лучший вариант |
| 6 | Спецфункции / формат Configure | ❓ Не определено | Требуется постановка от заказчика |
| — | Турбомашины | ➖ Исключено | Не делать |

Дополнительно восстановлены отсутствовавшие в репозитории модули:
- `su2_autoconfig.py` — устойчивые пресеты (safe/ultra) + детектор расхождения по history.csv;
- `su2_config_dialog.py` — окно настроек config.cfg (подсказки по параметрам, пресеты, откат .orig) + меню «SU2» + авто-предложение восстановления после падения расчёта (проводка в `SessionRunner._recover` уже была — теперь она заработает).

---

## 2. Детально по приоритетам ТЗ

### Высокий приоритет — Оптимизация формы (требование 5)

**Было:** случайный поиск 6 кандидатов вокруг стартовых параметров.

**Стало (DOE — табличный перебор):**
- На странице оптимизации — группа **«Табличная оптимизация (перебор вариантов)»**:
  - таблица вариантов (Размах / Хорда корня / Хорда конца / Стреловидность);
  - кнопки: «➕ Из текущих параметров», «➕ Ввести вручную…», «🗑 Удалить выбранное», «🧹 Очистить»;
  - кнопка «🔁 ЗАПУСТИТЬ ПЕРЕБОР»: каждая строка = один полный прогон (геометрия перестраивается, сетка перегенерируется, расчёт по выбранным полётным точкам);
  - таблица результатов (вариант → Cl, K, статус) заполняется по мере счёта;
  - по завершении лучший вариант применяется к генератору крыла.
- Бэкенд: `OptimizationWorker` получил параметр `candidates` (список dict-параметров) и сигнал `variant_ready`. Если `candidates` передан — идёт табличный перебор; если нет — прежний случайный поиск (поведение не сломано).
- Правила RuleSet (жёсткие/мягкие) и многоточечность (взлёт/крейсер/посадка) работают в обоих режимах.
- Таблица DOE сохраняется в проекте (`.aop`/JSON).

### Средний приоритет — Аэроупругость (Flutter)

- В коде отсутствует. SU2 не решает флатер напрямую; обычно нужен связный анализ (SU2 + структура) или постобработка.
- Рекомендация: отложить; в UI можно зарезервировать узел дерева «Aeroelasticity (планируется)».

### Низкий приоритет — Прочность

- Отсутствует. В перспективе — экспорт нагрузок из SU2 (surface_flow.vtu) в FEA-формат.

---

## 3. Детально по требованиям 1–4

### 3.1. Вычислительная эффективность и параллелизация (требование 1)

**Было:** слайдеры нагрузки CPU/GPU, `mpiexec -n N`, SU2_PARTITION, гибрид CPU+GPU, INNER_ITER по качеству сетки, RAMP_AOA.

**Добавлено в этой итерации:**
- **Подсказка по числу ядер** при старте расчёта: читается `NPOIN` из mesh.su2 → рекомендация ≈ N/150000 ядер (лог).
- **ETA до конца серии** в `add_result`: среднее время на точку × оставшиеся точки («⏱ Точка 2/5: среднее 95 с/точка, осталось ~5м»).

### 3.2. Плоскость симметрии (требование 2) — уже было реализовано

Полный цикл: UI → `get_symmetry_planes()` → `mesh/gmsh_generator.py` (маркеры) → `solver/config_builder.py` (MARKER_SYM) с защитой от отсутствия маркера в сетке. Осталось прогнать контрольный расчёт и зафиксировать ускорение 1.8–7×.

### 3.3. Адаптивная сетка (требование 3) — реализовано

- `solver/workers.py`:
  - `find_su2_adapt_exe()` — поиск SU2_ADAPT рядом с SU2_CFD.exe / в $SU2_HOME / в PATH;
  - `run_su2_adapt(case_dir, mesh_path, restart_path, adapt_markers, abs_error, log_cb)` — копирует mesh+restart в каталог кейса, пишет `adapt.cfg` (MARKER_ADAPT, ADAPT_ABS_ERROR, ADAPT_BOUNDARY), запускает SU2_ADAPT, возвращает путь к `mesh_adapt.su2`;
  - `_mesh_npoin()` — число точек mesh.su2 (для лога «до/после»).
- `mesh/mesh_worker.py: MeshAdaptWorker` — фоновый поток адаптации (UI не зависает).
- `ui/main_window.py` — кнопка **«🧬 Адаптировать сетку (SU2_ADAPT)»** на странице Mesh:
  1. проверяет наличие mesh.su2 и свежего `restart.dat` (из work/case_*/aoa_*/);
  2. спрашивает подтверждение замены рабочей сетки;
  3. запускает адаптацию, заменяет mesh.su2, показывает «точки до → после».
- Ограничение честно отражено в UI: нужен полный дистрибутив SU2 с модулем SU2_ADAPT (обычно входит в комплект). Если exe не найден — понятное сообщение.

### 3.4. Direct CAD Import (требование 4) — реализовано

- `geometry/generators.py: cad_to_stl(src, out, log)` — конвертация через gmsh (OpenCascade): `gmsh.open` → mesh поверхностей → STL. Поддерживаемые расширения: `.step .stp .iges .igs .x_t .x_b .sat .brep .bdf .nas .ply .obj .off` (`CAD_EXTENSIONS`).
- `ui/main_window.py: _add_body` — если расширение CAD: конвертация в `work/_cad_<id>_<имя>.stl`, затем обычный путь (триангуляция → тело в список). Имя тела — оригинальное имя файла.
- Новых зависимостей нет: gmsh уже используется приложением для объёмной сетки.
- Многодетальные сборки: каждая загруженная модель становится телом в списке (роль назначается пользователем). «Сборка как деталь» в STEP — работа в CAD-системе пользователя; со стороны AeroOpt сборка импортируется как единая триангуляция.

---

## 4. Прочие пункты ТЗ

- **Спецфункции** и **Формат Configure**: в коде и в ТЗ понимания нет — нужна постановка. (Рядом по смыслу стоит `su2_config_dialog.py` — окно настроек config.cfg; но это не «формат Configure» в смысле ТЗ.)
- **Турбомашины**: ничего турбомашинного нет — соответствует исключению.

---

## 5. Гигиена репозитория (итерация 1)

### 5.1. Удалено (мусор; на сборку .exe не влияет)

| Файл | Почему |
|---|---|
| 26 файлов `__pycache__/*.pyc` в git | бинарные артефакты; добавлен `.gitignore` |
| `tests/Python file.py` | пустой (0 байт), имя с пробелом |
| `OptimizationWorker/postprocess.py` + папка | пустой, нигде не импортируется |
| `ui/utils/helpers.py`, `ui/utils/__init__.py` | пустые, `ui.utils` нигде не импортируется |
| `su2_runner.py` | устаревший консольный runner (input/print), импортировал несуществующие `su2_autoconfig`/`template.cfg` |
| `sig_test.py` | ⚠️ содержал **HMAC-секрет сервера лицензий и лицензионный ключ открытым текстом** |
| `diagnose_license.py` | dev-инструмент (PyQt6), удалён по указанию пользователя |

### 5.2. ⚠️ Важно (повтор из итерации 1)

`sig_test.py` содержал открытым текстом `SECRET` (HMAC-ключ подписи ответов сервера лицензий) и ключ `AERO-M3A4-L7T7-3FAA-874D`. Файл удалён, но он уже был в истории git. Если репозиторий публичный или уходил наружу — **сменить HMAC-секрет на сервере** (`LICENSE_HMAC_KEY`), ключ при желании перевыпустить. Механизм лицензирования (`license_client/`) не трогался.

### 5.3. Итерация 2 (текущая)

- **Добавлены в корень:** `su2_autoconfig.py`, `su2_config_dialog.py` (восстановление отсутствовавших модулей; см. §1).
- **Удалён:** `diagnose_license.py` (по указанию пользователя).
- **Про сборку .exe:** `build_exe.py` **не менялся** — оба модуля импортируются статически (`su2_gui.py`, `solver/workers.py`), PyInstaller с `--paths={корень}` подхватит их автоматически. Правки — только в существующих файлах + 2 новых модуля, которые и так обязаны быть в бандле.

---

## 6. Что было починено (итерация 1)

- `ui/main_window.py` — мёртвый `_on_cpu_slider_changed()` обращался к несуществующим атрибутам; переписан на рабочий вариант.
- `tests/test_backend.py` — 4 устаревших ожидания (форматирование чисел); теперь зелёные.
- `tests/smoke_import.py`, `tests/qt_stubs.py` — добавлены стабы `QToolTip`, `QLockFile`.
- `tests/ast_lint.py` — чист (3 ложных срабатывания на мёртвом коде ушли).

---

## 7. Предлагаемый план работ (обновлён)

| # | Работа | Статус |
|---|---|---|
| 1 | Вернуть `su2_autoconfig.py` / `su2_config_dialog.py` | ✅ сделано (файлы от пользователя) |
| 2 | Табличная оптимизация DOE | ✅ сделано |
| 3 | Direct CAD Import через gmsh | ✅ сделано |
| 4 | Адаптивная сетка (SU2_ADAPT) | ✅ сделано |
| 5 | Автоподбор ядер + ETA серии | ✅ сделано |
| 6 | Контрольный замер ускорения от симметрии | ⏳ тест на реальной задаче пользователя |
| 7 | Спецфункции / Configure | ⏳ ждём постановку |

**Осталось на будущее (по желанию):**
- Постановка «Спецфункции» и «Формат Configure».
- Поколения в DOE (наследование/мутация строк таблицы), сетка значений параметров (автогенерация строк по шагам).
- Адаптация через gmsh (второй путь, если SU2_ADAPT недоступен): поле размера по градиенту давления с грубого расчёта.
- Резерв узла дерева «Aeroelasticity (планируется)».

**Важно по сборке:** новые модули `su2_autoconfig.py` и `su2_config_dialog.py` должны попасть в бандл — они попадут автоматически (статический импорт). Но после добавления любых *неимпортируемых* файлов `build_exe.py` придётся трогать — сейчас таких нет.
